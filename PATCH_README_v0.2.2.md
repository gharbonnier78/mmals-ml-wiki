# Diderot v0.2.2 minimal patch

Apply this patch on top of the current `mmals-ml-wiki` repository. It updates only the files needed to register the public `mmals-ecospec` repository and EcoSpec branch.

## Main update

`mmals-ecospec` is now tracked as public, active, and linked to the EcoSpec 1.0 specification branch.

## Suggested commit

```text
fix: register mmals-ecospec as public EcoSpec repository

- add mmals-ecospec to the Diderot repository registry
- add EcoSpec 1.0 as an active public specification branch
- map EcoSpec to TPUT, goal-adaptive control, RC2O, functional memory, geometry, and CAL evidence governance
- add EcoSpec to the publication registry with HAL/arXiv/DOI still pending
- add stable /go/ecospec article link and compact concept page
- update branch/repository/publication pages and registry counts
```
