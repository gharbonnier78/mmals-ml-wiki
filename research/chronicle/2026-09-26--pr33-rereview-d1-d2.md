# Chronicle — 2026-09-26 — PR #33 rereview formula and source-sync repair

Status: **append-only pedagogical amendment**

## Independent rereview disposition

The independent rereview of Diderot PR #33 at head `03b3fb1` returned **PARTIAL ACCEPT**.

The reviewer accepted the two new concepts, notation and provenance, but found two remaining issues:

- **D1:** five learner pages contained broken TeX because backslashes had been dropped and one `\theta` sequence had become a tab plus `heta`;
- **D2:** hand-maintained learner pages could drift from the canonical `data/concepts.json` entries.

## D1 correction

The forecasting concept pages were resynchronized with the canonical formulas in `data/concepts.json`.

The rendering-contract check now fails on the forecasting pages if a formula contains:

- control characters such as a literal tab;
- high-signal bare TeX command names such as `mathbb`, `mathcal`, `ldots`, `mid`, `approx`, `Omega` or `theta` without their leading backslash;
- no exact occurrence of the canonical formula registered in `data/concepts.json`.

The proper-scoring page now separates:

- canonical propriety inequality as the formal anchor;
- the expected normalized Brier-loss example as a second formula.

## D2 correction

For the eight collective-forecasting concepts touched by this work, CI now checks that the learner-facing page contains the canonical:

- summary;
- Discover text;
- Engineer text;
- Research text;
- formula

from `data/concepts.json`.

This does not make all legacy Diderot pages generated from JSON; it creates a fail-closed consistency contract for this forecasting concept family and prevents the silent dual-copy drift identified by the reviewer.

## Scientific/pedagogical boundary

No empirical scientific claim changes. Meta-prediction and the Brier diversity identity remain source-derived teaching concepts; Forecast Contract remains project-local; Prediction Market remains outside Engineering Collective Forecasting Study 0.

## Next admissible action

Run exact-head link/render/browser checks, then request bounded verification of D1/D2. PR #33 remains dependent on merging Engineering Collective Forecasting PR #1 first, after which its project source note must be re-pinned to the merged commit before Diderot merge.
